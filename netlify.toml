[build]
  functions = "api"
  command = "echo 'No build step required'"

[functions]
  directory = "api"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[dev]
  command = "echo 'Dev server ready'"
  port = 8888
  publish = "public"